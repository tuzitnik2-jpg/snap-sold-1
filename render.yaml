services:
  - type: web
    name: snap-sold
    runtime: node
    plan: free
    buildCommand: "echo 'no deps needed'"
    startCommand: "node server.js"
    envVars:
      - key: NODE_VERSION
        value: 20
